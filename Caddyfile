{
    # Use Caddy's local CA to issue certificates (no external CA required)
    local_certs
}

django-cms.org.127.0.0.1.nip.io, localhost {
    tls internal

    handle /static/* {
        root * /srv
        file_server
    }

    reverse_proxy web:80
}